class Report
{
    public void GenerateReport(User user)
    {
        Console.WriteLine("Generating report for " + user.Name);
        // Generate and print the report based on user's financial data
    }
}